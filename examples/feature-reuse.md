---
kind: example
validation_status: not-run
synthetic: true
---

# 示例：本地已有可复用功能

虚构演示，未执行命令、搜索或验证，不是实测结果。

调用：Codex 使用 `$reuse-scout`，Claude Code 使用 `/reuse-scout`，随后附上相同请求：

```text
快速模式。为批量导入寻找可复用的邮箱规范化实现，只研究，不修改代码。
```

假设本地已提供 `src/normalize.py` 的 `normalize_email` 与测试定义，覆盖空格处理和大小写规则；导入接口要求与它一致。

预期报告示意：建议复用 `normalize_email`，在导入入口调用现有接口；无需为同一功能再引包。证据是上述本地函数及测试定义，属于只读代码证据；调用方仍需核对空值约定，是适配推断。测试未执行，不能据此宣布功能正确。

最小计划：确认空值约定 → 复用现有函数 → 获准实施后用正常、空值及非法邮箱输入验证。拟议验证命令为 `python -m unittest tests.test_normalize`，可能生成缓存，当前不运行。

假设实际工具轨迹确实只有上述本地读取，报告才可填写查询 0/3、详情 0/5、深入候选 1/2；本示例并未产生工具轨迹。停止原因：本地证据足够且用户没有要求外部比较。
