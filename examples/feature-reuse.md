---
kind: example
validation_status: not-run
synthetic: true
---

# 示例：本地已有可复用功能

虚构演示，未执行命令、搜索或验证，不是实测结果。

调用：Codex 使用 $reuse-scout，Claude Code 使用 /reuse-scout，随后附上相同请求：快速模式。为批量导入寻找可复用的邮箱规范化实现，只研究，不修改代码。

假设本地已提供 src/normalize.py 的 normalize_email 与测试定义，覆盖空格处理和大小写规则；导入接口要求与它一致。

预期简短报告：建议复用 normalize_email，在导入入口调用现有接口。证据性质为**本地静态阅读**：函数和测试定义可定位，但只说明代码写了什么。空值约定仍属**适配推断**，需要核对调用方。**执行状态：未执行**；测试文件存在不能写“本地验证通过”。

最小计划：确认空值约定，获准实施后复用现有函数，再用正常、空值及非法邮箱输入验证。拟议命令为 python -m unittest tests.test_normalize，可能生成缓存，本示例不运行。

只有实际轨迹确认本地早停后，才填写查询 0/3、详情 0/5；本示例没有工具轨迹。停止条件为本地证据足够且用户未要求外部比较。
