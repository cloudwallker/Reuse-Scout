---
kind: example
validation_status: not-run
synthetic: true
---

# 示例：离线且许可未知

虚构演示，未执行命令、联网或验证。

调用：Codex 使用 $reuse-scout，Claude Code 使用 /reuse-scout，随后附上相同请求：离线模式。只根据当前仓库和我给的资料判断这个解析库是否适用，禁止联网。

假设仅提供锁文件版本 parser-demo 1.2 和旧 API 摘录，没有目标平台支持说明或明确许可文件。

预期简短报告：暂不决策。锁文件属于**本地静态阅读**，只能定位当前声明版本；旧摘录不能证实目标平台，许可状态为**证据不足**，不能建议复制或引入代码。是否可薄层适配仍是**适配推断**。**执行状态：未执行**；没有本地验证结果。

查询 0/0、详情 0/0 是离线模式约束；即使已有 URL，也不联网核查。最小下一步是取得可在授权范围内阅读的相应版本文档和明确许可，再安排获准的本地验证。没有搜索结果不等于没有可用方案。
