---
kind: example
validation_status: not-run
synthetic: true
---

# 示例：离线且证据不足

虚构演示，未执行命令、联网或验证。

调用：Codex 使用 `$reuse-scout`，Claude Code 使用 `/reuse-scout`，随后附上相同请求：

```text
离线模式。只根据当前仓库和我给的资料判断这个解析库是否适用，禁止联网。
```

假设仅提供锁文件版本 `parser-demo 1.2` 和一份旧 API 摘录，缺少目标平台支持与许可证。

预期报告示意：暂不决策。锁文件能定位当前版本，旧摘录不足以确认平台与许可，不直接建议复制/引入代码。先补充本地已获授权的许可文件和相应版本文档，再决定直接复用还是薄层适配。

验证状态：适配推断，未执行本地验证。外部查询 0/0、详情 0/0 是此场景要求，不能打开已知 URL 来确认链接；深入评估只限已有材料。停止原因：离线限制和关键证据缺失。没有搜索结果不等于没有可用方案。
